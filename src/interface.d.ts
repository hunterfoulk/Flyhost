interface CloseModal {
  closeModal: () => void;
}

interface File {
  user_id: Number;
  date: String;
  title: String;
}

interface handleSearch {
  handleSearch: () => void;
}

handleCurrent;
interface handleCurrent {
  handleCurrent: () => void;
}
